<%= form_with(model: task, local: true) do |f| %>
  <%= f.label :content, 'タスクを入力' %>
  <%= f.text_field :content %>
  
  
  <% if @task.errors.any? %>
   <p>※何か入力してください。</p>
  <% end %>
  <%= f.submit '登録' %>
<% end %>